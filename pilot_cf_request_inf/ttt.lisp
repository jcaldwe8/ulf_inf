; This is for separately loading Adam Purtee's relocatable version of ttt
;
; Note that currently (Oct 2013) init.lisp doesn't load parser.lisp
; or ttt -- eventually they should be loaded there for complete
; sentence processing.

(load "/p/nl/tools/ttt/src/load")
