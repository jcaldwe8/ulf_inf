;;; Gene Kim 11/20/2017
;;; File contains world knowledge (separate from purely lexical knowledge).

(defparameter *world-knowledge*
  (list
    ;; Tenors are people (at least in the context we're looking at...
    ;; See if we can get this from WordNet
    '(/ tenor.n person.n)))

